# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: build
on:
  push: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      CI: "true"
      TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref_name }}
          repository: ${{ github.repository }}
      - run: echo "foo446HelloBOOMBOOM1" > foo.txt
      - run: |-
          git config --list || echo "error"
          git config --list --global || echo "error"
          git config --list --system || echo "error"
      - run: |-
          if ! git diff --exit-code; then
            git config --global init.defaultBranch main
            git config user.name "Elad Ben-Israel"
            git config user.email "elad.benisrael@gmail.com"
            git add .
            git commit -m "chore: self mutation"
            git push https://boom:${TOKEN}@github.com/${{ github.repository }}.git HEAD:${{ github.ref_name }}
          fi
