# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: build
on:
  pull_request: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          token: ${{ secrets.PROJEN_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
      - run: echo "foo446HelloBOOMBOOM2BAAAM" > foo.txt
      - run: |-
          if ! git diff --exit-code; then
            git config user.name "Elad Ben-Israel"
            git config user.email "elad.benisrael@gmail.com"
            git add .
            git commit -m "chore: self mutation"
            git push origin HEAD:${{ github.ref_name }}
          fi
